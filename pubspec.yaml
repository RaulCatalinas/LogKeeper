name: logkeeper
description: Simple file logger that works out of the box with automatic timestamping and session management.
version: 1.0.0
repository: https://github.com/RaulCatalinas/Logkeeper

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  intl: ^0.20.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
